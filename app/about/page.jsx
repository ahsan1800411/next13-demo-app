// this is how we add the metadata to every page

export const metadata = {
  title: ' About App',
};

const AboutPage = () => {
  return <div>This is the About Page</div>;
};

export default AboutPage;
